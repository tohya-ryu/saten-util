// SATEN_BUILDER_SCENE_{{up}}
    case {{id}}:
        new = saten_scene_create(scene_{{low}}_init, scene_{{low}}_update,
                scene_{{low}}_draw, scene_{{low}}_quit, data);
        break;
    // SATEN_BUILDER_SCENE_NEXT_SRC
